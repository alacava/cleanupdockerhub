# ============================================================
# Docker Hub Credentials
# ============================================================

# Your Docker Hub username
DOCKERHUB_USERNAME=your-dockerhub-username

# Docker Hub Personal Access Token (recommended) or password.
# Create a token at: https://hub.docker.com/settings/security
DOCKERHUB_TOKEN=your-dockerhub-token

# Namespace to clean. Defaults to DOCKERHUB_USERNAME if not set.
# Set this to your organisation name when cleaning an org's repos.
# DOCKERHUB_NAMESPACE=your-org-or-username

# ============================================================
# Retention Policy
# ============================================================

# Number of most-recently-updated tags to keep unconditionally, per repository.
# Default: 5
KEEP_LAST_N=5

# Minimum age (in days) a tag must reach before it is eligible for deletion.
# A tag is only deleted when BOTH it is outside KEEP_LAST_N AND older than this.
# Default: 30
MIN_AGE_DAYS=30

# ============================================================
# Scope
# ============================================================

# Comma-separated list of repository names to process.
# Leave empty (or omit) to process ALL repositories in the namespace.
# Example: REPOS_TO_CLEAN=my-api,my-frontend,my-worker
REPOS_TO_CLEAN=

# Comma-separated list of tags that are never deleted, regardless of age or rank.
# Default: latest
EXCLUDE_TAGS=latest

# ============================================================
# Safety
# ============================================================

# When DRY_RUN=true the tool only logs what it would delete — nothing is removed.
# Set to false only when you are confident the policy is correct.
# Default: true
DRY_RUN=true
